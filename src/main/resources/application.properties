spring.application.name=AVU

# Configuración del servidor
# Render usa la variable de entorno PORT, si no existe usa 8080 por defecto
server.port=${PORT:8080}

# Configuración de Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Configuración de base de datos (descomentar la que uses)
# Para MySQL:
# spring.datasource.url=jdbc:mysql://localhost:3306/tu_base_datos
# spring.datasource.username=tu_usuario
# spring.datasource.password=tu_contraseña
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# Para PostgreSQL:
# spring.datasource.url=jdbc:postgresql://localhost:5432/tu_base_datos
# spring.datasource.username=tu_usuario
# spring.datasource.password=tu_contraseña
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# H2 Database embebida para desarrollo (ya está configurada)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
